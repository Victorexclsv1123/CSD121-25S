Polymorphism and Generalization helps use additional player
without modifying the tictactoe class/main method. the subclasses  
of abstract is the HumanPlayer, Circe, or Omola. means it can
be use in what player expected. the game only has to use pickNextMove() to function.
it doesn't need to know if the player is a computer or a human. Because of this, adding 
additional player kinds to the code is simple and adaptable without changing the game's logic.